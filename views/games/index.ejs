<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
</head>
<body>

<%- include('../partials/_navbar.ejs') %>

<h1> <%= currentUser.username %> Stats </h1>

<h1> TOTALS </h1>
<h2> Games: <%= currentUser.totals.totalGames %> </h2>
<h2> Goals: <%= currentUser.totals.totalGoals %> </h2>
<h2> Assists: <%= currentUser.totals.totalAssists %> </h2>
<h2> Wins: <%= currentUser.totals.totalWins %> </h2>
<h2> Losses: <%= currentUser.totals.totalLosses %> </h2>
<h2> Draws: <%= currentUser.totals.totalDraws %> </h2>

<h1> AVERAGES </h1>
<h2> Goals: <%= currentUser.averages.avgGoals %> </h2>
<h2> Assists: <%= currentUser.averages.avgAssists %> </h2>
<h2> Win Rate: <%= currentUser.averages.winPercentage %> % </h2>

<ul>
<% gameStats.forEach((game) => { %>
    <li> <a href="/users/<%= user._id %>/games/<%= game._id %>">
        Game <%= game.number %> - <%= game.date %> - <%= game.result %> 
    </li> </a>
<% }) %>
</ul>






<a href="/users/<%= user._id %>/games/new">Add New Game Stats</a>


</body>
</html>
